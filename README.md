# manipy

Optimale Experimental Manipulation of Stimuli, models, pipelines, functions and utilities.

Install from source:

```bash
pip install .
```

Build locally:

```bash
python -m build
```

Or download latest version directly from pip:

```bash
pip install manipy
```
